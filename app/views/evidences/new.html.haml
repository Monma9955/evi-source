.container
  = render 'header'
  .main
    = simple_form_for @post, url: { controller: 'evidences', action: 'create' }, local: true do |f|
      = f.input :text, placeholder: "例) 農業、漁業、製造業、飲食業、小売業、作家・俳優業など幅広い業種で、法人・個人の方が対象"

      = f.fields_for :evidences do |evidence|
        = evidence.input :source, placeholder: "例) https://..."
        = evidence.input :informant, placeholder: "例) 経済産業省"
        = evidence.input :source_updated_on, start_year: 1900, end_year: Date.current.year
        .form-container
          = evidence.input :level
          = evidence.input :fact_or_opinion
        -# fact_or_opinionを選択したら表示させる
        #addDataType
          = evidence.input :data_type
        = evidence.input :reference, placeholder: "例) 詳細は「持続化給付金に関するお知らせ」 「制度の概要」欄をご一読ください。"
      = f.button :submit
